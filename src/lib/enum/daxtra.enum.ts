export enum DaxtraStatus {
    SUCCESS = 'success',
    FAILED = 'failed',
    NOT_READY = 'not_ready',
    DISABLED = 'disabled',
}
export enum DaxtraErrorCode {
    TIMED_OUT = 'daxtra_timed_out',
    FAILED_TO_PARSE_RESUME = 'daxtra_failed_to_parse_resume',
}
